Striped placeholder for missing imagery; always says what should replace it.

```jsx
<PlaceholderMedia height={620} label="[ hero product shot — dark dashboard render ]" radius="var(--radius-lg)" />
<PlaceholderMedia dark height={420} label="[ early concept explorations ]" />
```
